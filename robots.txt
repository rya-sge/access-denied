# ---------------------------------------------------------------------------
# IMPORTANT — this file is currently NOT the robots.txt crawlers actually read.
#
# robots.txt is only honoured at the ROOT of a host (RFC 9309). This site is a
# GitHub Pages *project* site published at:
#
#     https://rya-sge.github.io/access-denied/
#
# so this file is served at https://rya-sge.github.io/access-denied/robots.txt,
# which crawlers ignore. They fetch https://rya-sge.github.io/robots.txt, which
# comes from the `rya-sge.github.io` user-page repository instead.
#
# => To make the AI-crawler rules below actually take effect, copy them into
#    the robots.txt at the root of the `rya-sge.github.io` repository, or move
#    this blog onto its own custom domain.
#
# The rules are kept here so they are version-controlled next to the site and
# become effective automatically if it ever moves to its own domain.
# ---------------------------------------------------------------------------

# Block AI training / dataset crawlers.
# Note: Google-Extended and Applebot-Extended only opt out of AI training —
# they do not affect Google Search or Siri indexing, so rankings are unaffected.
User-agent: GPTBot
Disallow: /

User-agent: Google-Extended
Disallow: /

User-agent: ClaudeBot
Disallow: /

User-agent: Applebot-Extended
Disallow: /

User-agent: Meta-ExternalAgent
Disallow: /

User-agent: Meta-ExternalFetcher
Disallow: /

User-agent: Amazonbot
Disallow: /

# Everything else may crawl the whole site.
User-agent: *
Disallow:

# Generated by jekyll-sitemap. Absolute URL is required by the standard.
# Until this file sits at the host root (see the note above), submit the same
# URL manually in Google Search Console and Bing Webmaster Tools — both are
# already verified for this site (googlebed50df54eff8961.html, BingSiteAuth.xml).
Sitemap: https://rya-sge.github.io/access-denied/sitemap.xml
